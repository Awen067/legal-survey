# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# Generated output
output/
*.db

# Backup files
server_backup.py
*_backup.py

# Static deploy version (not needed for server)
deploy/

# Railway detection: these files mislead Railway into detecting "shell" instead of "python"
# Keep them locally (start.sh for local use), but exclude from git/Railway
start.sh
Procfile
runtime.txt

# IDE
.vscode/
.idea/
